{
  "name": "mera-rozgar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm:dev -w client\" \"npm:dev -w server\"",
    "build": "npm run build -w client",
    "check": "npm run check -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
